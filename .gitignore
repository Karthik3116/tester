# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
.npm/

# Environment variables
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Build artifacts
dist/
build/
*.log
*.tmp
*.bak
*.swp
coverage/

# IDE and Editor files
.vscode/        # VSCode
.idea/          # IntelliJ IDEA
*.sublime-project
*.sublime-workspace
.DS_Store       # macOS
Thumbs.db       # Windows

# MongoDB specific (if any local DB files are created outside of data directory)
*.sqlite
*.db
*.db-journal

# Render specific files (if any)
.render/

# Vercel specific files (if any)
.vercel/

# Other common ignored files
ehcache/
target/
*.iml
.classpath
.project
.settings/
.factorypath

# Explicitly ignore node_modules and dist/ within subdirectories
# This is often redundant if node_modules/ and dist/ are at root,
# but can be useful if they are not consistently at the root or if
# you have nested projects.
backend/node_modules/
backend/dist/ # If your backend builds anything to dist
frontend/node_modules/
frontend/dist/
